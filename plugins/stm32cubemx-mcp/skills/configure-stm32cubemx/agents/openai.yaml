interface:
  display_name: "Configure STM32CubeMX"
  short_description: "Create and validate STM32CubeMX configurations"
  default_prompt: "Use $configure-stm32cubemx to create or inspect an STM32 configuration and plan the requested changes safely."
